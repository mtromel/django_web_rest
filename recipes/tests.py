"""Tests for recipe URL routes."""

from django.test import TestCase


class RecipeURLsTest(TestCase):
    """Tests for recipe URL routes."""

    def test_the_pytest_is_ok(self):
        """Test that pytest is working."""
        assert 1 == 1, 'Um é igual a um'
